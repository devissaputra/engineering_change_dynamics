[project]
name = "engineering-change-dynamics"
version = "1.1.0"
description = "Cross-project maintenance-intent heterogeneity study using manually classified Apache commits."
requires-python = ">=3.10"
dependencies = ["scipy>=1.11,<2"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
